#!/usr/bin/python
# Post-update
#  -sets proper flags (chmod +x on some scripts) on some files that might have been recreated

import sys;
import os
import subprocess;
import shutil,stat


os.chmod("bin/initializedb.py",0744)
print("Set chmod +x on initializedb.py")
